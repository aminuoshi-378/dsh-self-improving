/** Host loader entry for the Experiences browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the Experiences tab. */
export function apply(): void {}
