/** Host loader entry for the Rules-editor browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the Rules editor tab. */
export function apply(): void {}