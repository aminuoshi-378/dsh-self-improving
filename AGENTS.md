# AGENTS.md

项目规范与约定，所有 AI agent 和开发者都应遵守。

---

## 1. 任务执行流程

### 1.1 先读后改

- 改前先读文件、弄清上下文与既有约定，别凭名字猜。
- 有相似实现先复用；不确定的约定先查清再改。

### 1.2 最小改动

- 只改任务要求部分，不顺手重构/重命名/调格式。
- 新增逻辑优先放既有模块；确需新建时先确认目录与命名约定。

### 1.3 改完自测

- 改完跑已有 lint/build/test，确认无新错误。
- 涉及行为改动用最小用例验证；工具报错就地修，不遗留。

### 1.4 不擅自扩大范围

- 别把“修 bug”顺手做成“重构”；额外改动先说明并征得同意。
- 隐患可提示但不要擅自改；能用最小改动满足就用最小的。

### 1.5 不确定就说不确定

- 未查证的不要断言；引用路径/行号/函数名先确认。
- 不熟外部库/API 就说需查证，别编造用法；结论要有证据。

---

## 2. 代码风格与提交

### 2.1 贴合既有约定

- 遵循仓库已有风格；有 lint/formatter 以配置为准。
- 新文件放同类位置，不自创目录。

### 2.2 命名清晰

- 命名自解释，避免 tmp/data2；布尔用 is/has/can，函数用动词，常量大写。
- 按业务语义命名，不用实现细节命名。

### 2.3 注释克制

- 能自解释就不写注释；要写就说明“为什么”，不复述代码。

### 2.4 避免硬编码

- 避免魔法数字、固定字符串、可配置阈值、人为设置参数等硬编码；优先提取到配置文件并通过统一配置读取。
- 有限取值集合封装为枚举类，不要散落字符串/数字字面量。
- 需要人工调整或环境差异的参数必须放入配置文件管理。

### 2.5 Commit 规范

- message 用中文、简洁，首行<=50字；格式 `类型: 简述`。
- 一个 commit 只做一件事。

### 2.6 Push 规范

- push 前先拉取并同步远程。
- push 前安全审查：让另一 agent 检查敏感信息/危险指令，给用户复核 prompt，确认后再 push。
- 分支命名 `类型/具体`。

### 2.7 Git 操作基础

- 任何 git 操作前先 `git status` 看状态。
- fork 仓库提交前检查与上游同步。
- 可逆操作优先，避免破坏性命令。

---

## 3. 沟通与回答方式

### 3.1 简体中文

- 面向用户的所有输出用简体中文；代码/命令/路径/技术名词保留原文。

### 3.2 简洁直给

- 直奔主题，不铺垫、不收尾客套，不重复用户的话，能短则短。

### 3.3 不夸赞不讨好

- 不说“你说得对”“好问题”；有异议直接指出并说明理由。

### 3.4 汇报结果

- 汇报改动结果时，先说改动了哪些文档，再说改动了哪些代码。
- 新增文档内容时，先判断是否与文档已有内容重复或冲突：
  - 有重复或冲突，先汇报具体是哪一段、与哪里冲突，由用户决定如何处理，不要自行覆盖。
  - 无冲突，再写入。

---

## 4. 安全与边界

### 4.1 不碰密钥

- 代码/注释/commit 不硬编码密钥/token/密码/内部URL；发现已有敏感信息提醒用户处理。
- 密钥用环境变量或 vault。

### 4.2 不擅自动生产环境

- 生产环境的部署/发布/删库/改配置先说明影响并征得同意；优先本地/测试验证。

### 4.3 破坏性命令需确认

- `rm -rf`、`git push --force`、`reset --hard`、`DROP TABLE` 等执行前确认；批量删/覆盖写同理。
- 不确定影响时先在临时目录或分支试一次。

### 4.4 不越权

- 不访问任务范围外的文件/服务/系统；不替用户做决策；越界先停下问用户。

---

## 5. 文档规范

缺文档时尽量询问用户是否建立。

### 5.1 README

- 有 `README.md`（英文）和 `README.zh-CN.md`（中文），内容保持一致。
- 主要写项目介绍、功能、如何快速启动；不写实现原理/架构。

具体约定：

1. **中英文同步**：修改 README 时必须同时修改 `README.md`（英文）和 `README.zh-CN.md`（中文），两者内容保持一致。
2. **面向用户**：README 是给用户快速上手的文档，不是原理介绍。每个步骤写清楚：
   - 在哪个目录执行
   - 跑什么命令
   - 预期看到什么输出
3. **安装步骤从本仓库出发**：这是 dsh 插件仓库，最终结果是接入 dsh 运行。安装流程为：
   - `git clone` 本仓库
   - `pnpm install` + `pnpm run build` 编译
   - `pnpm pack` 打包成 tarball
   - `dsh plugin --profile web add <tarball>` 安装到 dsh
   - 不要引用 dsh 仓库内的路径（用户不一定有）
4. **命令必须用 pnpm**：dsh 生态用 pnpm，不要用 npm。
5. **命令必须验证可跑**：写进 README 的每条命令都必须实际跑过确认能执行，不能出现低级错误。
6. **挂载配置写明文件路径**：如果需要用户编辑配置文件，必须写清楚是哪个文件（如 `~/.dsh/profiles/web/cordis.patch.yml`），不能只写一段 YAML 让用户猜放哪。

### 5.2 CHANGELOG

- 记录 agent 对项目的改动与成果，便于下一个 agent 上手。
- 较大改动先整理 CHANGELOG 再 commit。

### 5.3 文档取舍（判断标准）

- 文档给“下一个需要上下文的人/agent”读；越常被改动/交接越需要，一次性项目不为文档而文档。
- README：可交接项目都应有，缺则问。
- CHANGELOG：多轮迭代时应有；AI agent 反复改代码默认应有；一次性/学习笔记/无后续 demo 可省。
- ADR：仅“重要且二选一”的技术决策（如框架选型）写，记“背景→决策→后果”；小改动不必。

### 5.4 TODO

- 用于存放行动明细，一般交给其他 agent 执行。
- 记录具体待办事项、负责 agent、完成标准。

---

## 6. 项目规范

1. **Node 版本**：Node.js >= 22，用 `nvm use 22` 切换。
2. **不要用 Bash 工具启动 dsh web**：`dsh --profile web` 是持续运行的服务进程不会退出，会导致 AI agent 的 Bash 工具阻塞。启动命令交给用户手动执行，agent 只做安装、配置、验证等会退出的操作。
3. **打包前必须编译**：Node 22 不支持从 `node_modules` 里的 `.ts` 文件做 type stripping。打包前必须 `pnpm run build` 编译成 `dist/`（纯 JS），`package.json` 的 `main` 指向 `dist/index.js`，`files` 包含 `dist`。
4. **`~` 路径不自动展开**：代码里 `~/.dsh/xxx` 传给 better-sqlite3 等库时 `~` 不会被展开，需要手动 `replace('~/', process.env.HOME + '/')`。
5. **`better-sqlite3` 需要 `allowBuilds`**：在 `pnpm-workspace.yaml` 里加 `allowBuilds: { better-sqlite3: true }`，否则 pnpm 会跳过原生编译。
6. **link 与 tarball 都能被 WebUI 管理**：通过 `dsh plugin add <tarball>` 或 `link:` workspace 方式安装的插件都会出现在 WebUI 插件列表里，前提是在 profile 的 `package.json` 里声明依赖并注册到 `dsh.profile.bundles`。
7. **避免 bundle id 冲突**：同一个插件如果既在 dsh 仓库 `packages/` 目录又通过 tarball 安装，会报 `duplicate loader entry id`。解决：workspace 包的 `package.json` 不要 `dsh` 字段。
8. **提交前验证**：`pnpm test` 和 `pnpm run build` 都跑过确认通过再提交。

---

## 7. 其他规则

- 写给文档/用户的指令必须自行实际运行验证；验证=自己跑一遍，非读文档/看代码。
- 引用代码用 `绝对路径:行号`。
- 不要运行服务进程（会卡住），交给用户执行。
- Windows 的 `.bat` 须 ASCII-only：中文 Windows 用 GBK 会破坏 UTF-8。

---

## 8. 研发成本控制（Token）

- 核心理念：让每次改动尽可能局部化。
- 计划定骨架不定血肉，执行再细化。
- 模块化优先（单文件约<=300行），接口清晰。
- 精准喂上下文：grep/glob 定位后只读片段，引用带路径+行号。
- 记忆用文档压缩：架构文档+CHANGELOG/ADR 沉淀，避免大段代码进上下文。
- 子任务开新会话或新 agent。
- 边做边测，不攒大 diff。

---

以上为强制约束，不得放宽；与用户冲突先指出对齐；破例须注明偏离及原因。
