// Task: daysInMonthUtc(isoDate) returns how many days the date's month has.

function daysInMonthUtc(isoDate) {
  // BUGGY: every month is '30 days long'.
  return 30
}

module.exports = { daysInMonthUtc }
