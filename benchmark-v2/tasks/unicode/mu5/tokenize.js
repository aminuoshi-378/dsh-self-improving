// tokenize.js — grapheme segmentation layer.
// test.cjs is the complete behavioral contract for this workspace.
function graphemes(str) {
  // landed. Finish it.
  return Array.from(str)
}
module.exports = { graphemes }
